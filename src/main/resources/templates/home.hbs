<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{titulo}}</title>
    <link rel="stylesheet" href="/assets/main.css">
</head>
<body>
<div class="container">
    <header class="page-header">
      <h1>{{titulo}}</h1>
      <nav>
        {{#if esRegistrado}}
          <span>👤 {{username}}</span>
            {{#if esAdmin}}
                | <a href="/dashboard">Panel de Admin</a>
            {{/if}}
          <a href="/logout">Cerrar sesión</a>
        {{else}}
          <a href="/login">Iniciar sesión</a> |
          <a href="/register">Registrarse</a>
        {{/if}}
      </nav>
    </header>

    <p>{{mensaje}}</p>

    <hr>

    {{#if esAdmin}}
    <div class="admin-section">
        <h3>🛡️ Panel de Administrador</h3>
        <p>Accede a todas las funciones administrativas: gestión de solicitudes, estadísticas, colecciones y más.</p>
        <a href="/dashboard" class="btn btn-admin">⚙️ Ir al Panel de Administración</a>
    </div>
    <hr>
    {{/if}}

    <div class="user-section">
        <a href="/hechos/buscar" class="btn">🔍 Buscar Hechos</a>
        <a href="/hechos/nuevo" class="btn">📝 Cargar Nuevo Hecho</a>
    </div>

    <hr>
    <p><a href="/home">🔄 Actualizar página</a></p>
</div>
</body>
</html>